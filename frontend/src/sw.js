// Custom Service Worker for Haritha Fashion World PWA
self.addEventListener('fetch', (event) => {
  // Let the browser handle all requests by default
  // Add custom caching strategies here as needed
})
